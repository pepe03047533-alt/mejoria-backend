# Opcional. Si no se define:
#   - en local: Vite hace proxy de /api → http://localhost:3001
#   - en producción: se usa el mismo dominio (www.mejoria.com.ar)
#
# Solo necesario si el API vive en otro host:
# VITE_API_URL=https://tu-backend.ejemplo.com
